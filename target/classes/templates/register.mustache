<!DOCTYPE html>
<html xmlns:=http://www.w3.org/1999/xhtml>
<head>
    {{> meta}}
    <title>Регистрация</title>
</head>
<body>
<div class="page-login__container container">
    <div class="mx-auto border my-5 py-3 px-3 form">
        <div class="my-3" align="center">
            <div class="my-3" align="center">
                <h3 class="page-login__name1">PassVault</h3>
                <h6 class="page-login__name2">Регистрация</h6>
            </div>
        </div>
        <form action="register" method="post">
            <div class="form-group">
                <label for="name">Логин</label>
                <input class="form-control" id="login" type="text" name="login">
                <div class="error">
                    {{#loginErrorMessage}}
                        <b>{{loginErrorMessage}}</b>
                    {{/loginErrorMessage}}
                </div>
            </div>
            <div class="form-group">
                <label for="name">Имя</label>
                <input class="form-control" id="name" type="text" name="name">
                <div class="error">
                    {{#nameErrorMessage}}
                        <b>{{nameErrorMessage}}</b>
                    {{/nameErrorMessage}}
                </div>
            </div>
            <div class="form-group">
                <label for="email">Email</label>
                <input class="form-control" id="email" type="text" name="email">
                <div class="error">
                    {{#emailErrorMessage}}
                        <b>{{emailErrorMessage}}</b>
                    {{/emailErrorMessage}}
                </div>
            </div>
            <div class="form-group">
                <label for="password">Пароль</label>
                <input class="form-control" id="password" type="password" name="password">
                <div class="error">
                    {{#passwordErrorMessage}}
                        <b>{{passwordErrorMessage}}</b>
                    {{/passwordErrorMessage}}
                </div>
            </div>
            <div class="form-group">
                <label for="password">Повторите пароль</label>
                <input class="form-control" id="passwordRepeat" type="password" name="passwordRepeat">
            </div>
            <div class="center">
                <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
                <input class="btn btn-primary login-btn" type="submit" value="Зарегистрироваться"/>
            </div>
        </form>
    </div>
</div>
</body>
</html>